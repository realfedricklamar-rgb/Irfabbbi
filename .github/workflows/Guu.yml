# .github/workflows/hosted-persistent-ssh.yml
name: Hosted Persistent SSH Session

on:
  workflow_dispatch:
    inputs:
      session_duration_minutes:
        description: 'Maximum duration for the SSH session (minutes, max 360)'
        required: true
        default: 360
        type: number

jobs:
  interactive_ssh_session:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
    - name: Configure SSH and Tmux Environment
      run: |
        sudo apt-get update && sudo apt-get install -y openssh-server tmux jq
        
        # Enable runtime user authentication configurations
        sudo sed -i 's/PasswordAuthentication yes/PasswordAuthentication no/' /etc/ssh/sshd_config
        sudo sed -i 's/#StrictHostKeyChecking ask/StrictHostKeyChecking no/' /etc/ssh/ssh_config
        
        # Force systemd to fully register configuration updates across active sockets
        sudo systemctl daemon-reload
        sudo systemctl restart ssh.socket ssh.service
        echo "✅ SSH daemon updated and running."
      shell: bash

    - name: Install and Configure Ngrok
      run: |
        sudo mkdir -p /usr/share/keyrings
        curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo gpg --dearmor -o /usr/share/keyrings/ngrok.gpg
        
        echo "deb [signed-by=/usr/share/keyrings/ngrok.gpg] https://ngrok-agent.s3.amazonaws.com/ buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
        sudo apt-get update && sudo apt-get install -y ngrok
        
        ngrok config add-authtoken "${{ secrets.NGROK_AUTHTOKEN }}"
      shell: bash

    - name: Authorize User Public Key
      run: |
        # Target the runner user directory instead of root to comply with security policies
        mkdir -p $HOME/.ssh
        
        # Inject your specific public key
        echo "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIOcnm0bL4n7JzFC85yX5Zxamk/0RZY9/6MMRUnh+rMz/" > $HOME/.ssh/authorized_keys
        
        # Set absolute POSIX permission compliance
        chmod 600 $HOME/.ssh/authorized_keys
        chmod 700 $HOME/.ssh
        chmod 755 $HOME
        
        echo "✅ Authorized your personal public key for standard runner access."
      shell: bash

    - name: Start Public Tunnel and Keep Session Alive
      run: |
        DURATION_MINUTES=${{ inputs.session_duration_minutes }}
        DURATION_SECONDS=$(( DURATION_MINUTES * 60 ))
        
        # Fire up Ngrok TCP tunnel pointing directly to the local port
        ngrok tcp 22 --log=stdout > /dev/null &
        sleep 5
        
        # Parse connection addresses safely
        PUBLIC_ADDRESS=$(curl -s http://localhost:4040/api/tunnels | jq -r '.tunnels[0].public_url')
        CLEAN_ADDRESS=$(echo $PUBLIC_ADDRESS | sed 's/tcp:\/\///')
        TUNNEL_HOST=$(echo $CLEAN_ADDRESS | cut -d':' -f1)
        TUNNEL_PORT=$(echo $CLEAN_ADDRESS | cut -d':' -f2)
        
        echo "=========================================================="
        echo "🚀 INTERACTIVE PUBLIC SSH SESSION ACTIVE"
        echo "=========================================================="
        echo "Public Host: $TUNNEL_HOST"
        echo "Public Port: $TUNNEL_PORT"
        echo "User:        runner"
        echo "Max Runtime: $DURATION_MINUTES minutes"
        echo "----------------------------------------------------------"
        echo "Instructions to Connect From Your PC:"
        echo "Run this command (replace 'path/to/key' if yours isn't default):"
        echo " "
        echo "   ssh -i /path/to/your/private_key runner@$TUNNEL_HOST -p $TUNNEL_PORT"
        echo " "
        echo "Once connected, attach to your persistent shell workspace:"
        echo "   tmux attach -t persistent_session"
        echo "=========================================================="
        
        # Start background tmux space
        tmux new-session -d -s persistent_session
        tmux send-keys -t persistent_session "echo 'Welcome to your persistent runner environment!'" C-m
        tmux send-keys -t persistent_session "bash" C-m
        
        # Sleep to preserve environment life
        sleep $DURATION_SECONDS || true
      shell: bash

    - name: Graceful Teardown
      if: always()
      run: |
        tmux kill-server 2>/dev/null || true
        sudo systemctl stop ssh.service 2>/dev/null || true
        pkill ngrok || true
